kretprobe:fragmentation_index{ 
//uncomment following lines to dump kernel stack

//@kstack = kstack();
//print(@kstack);
//clear(@kstack); 


@count = @count +1;
printf("\nfrag counter %llu index: %d\n",@count,retval); 
}
